#include <stdio.h>
#include <stdlib.h>
#include "localidad.h"
#define TAMANIO 3

int main()
{

    eLocalidad;
    eLocalidad.estado=1;
    eLocalidad.id=555;
    strcpy(eLocalidad.nombre, "maria");
    //printf("%s",eLocalidad);
    loc_mostrarUno(eLocalidad);

    void loc_mostrarUno (eLocalidad listado [], int arrayLegth)
    {

        int i;
        for (i=0;)
        {
            printf("%s",)
        }
    }
    listadoDeLocalidades [TAMANIO];
    {
        for (pi<TAMANIO)
        {
            listadoDeLocalidades [i].id=i*3;
            listadoDeLocalidades [i]=1;
            strcpy (listadoDeLocalidades [i], "maria");

        }
        loc_mostrarUno(listadoDeLocalidades[i]);
    }

}
